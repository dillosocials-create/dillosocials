const c=document.getElementById('carousel'),cards=[...c.querySelectorAll('article')],buttons=[...document.querySelectorAll('.filters button')];let down=false,start=0,left=0;function move(x){c.scrollBy({left:x,behavior:'smooth'})}c.addEventListener('pointerdown',e=>{down=true;start=e.clientX;left=c.scrollLeft;c.classList.add('dragging');c.setPointerCapture(e.pointerId)});c.addEventListener('pointermove',e=>{if(down)c.scrollLeft=left-(e.clientX-start)*1.15});['pointerup','pointercancel','pointerleave'].forEach(x=>c.addEventListener(x,()=>{down=false;c.classList.remove('dragging')}));buttons.forEach(b=>b.onclick=()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');cards.forEach(x=>x.classList.toggle('hidden',b.dataset.filter!=='all'&&x.dataset.category!==b.dataset.filter));c.scrollTo({left:0,behavior:'smooth'})});let auto=true;c.addEventListener('wheel',()=>auto=false,{once:true});c.addEventListener('pointerdown',()=>auto=false,{once:true});function loop(){if(auto&&!down&&document.visibilityState==='visible')c.scrollLeft+=.2;requestAnimationFrame(loop)}loop();document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth'})}}));